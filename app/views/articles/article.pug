article.article
  h3
    a(href='/articles/'+article._id, title=article.case_name) #{article.case_name.slice(0, 140)}
  p= article.body.slice(0, 400)

  .meta
    span.text-muted= formatDate(article.create_date)

    - if (article.user)
      | &nbsp; - &nbsp;
      span 創建者 &nbsp;

      - var name = article.user.name ? article.user.name : article.user.username

      a(href="/users/"+article.user._id)= name

    - if (article.tags)
      | &nbsp; - &nbsp;
      span 標籤 &nbsp;
      each tag in article.tags.split(',').slice(0, 10)
        i.text-muted.fa.fa-tag &nbsp;
        a.tag(href="/tags/"+tag)= tag
        | &nbsp;&nbsp;
